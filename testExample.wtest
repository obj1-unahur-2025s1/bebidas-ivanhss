import tito.*

describe "test de tito" {
    method inialize(){
        tito.consumir(5, terere)
    }
  test "tito tiene peso 710" {
    assert.equals(70, tito.peso())
  }
  test "tito tiene velocidad 4.1" {
    assert.equals(7, tito.velocidad())
  }
}
